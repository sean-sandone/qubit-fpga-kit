[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "qubit_sim"
version = "0.1.0"
dependencies = [
    "numpy",
    "qutip",
    "matplotlib",
    "pyserial",
    "flask",
]

[tool.setuptools.packages.find]
where = ["src"]
